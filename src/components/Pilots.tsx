import { TourInfo } from '../types';

function Pilots({ tourInfo }:{ tourInfo: any }) {
  const { name: planetName, residents: { name: pilotName } } = tourInfo;

  

  return null;
}

export default Pilots;
